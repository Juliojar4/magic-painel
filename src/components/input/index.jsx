import React from 'react';
import './style.css';

const Input = (props) => { 
    return (
        <div className="wrap-input">
            <input className="input-content"/>
        </div>
    );
};

export default Input;
